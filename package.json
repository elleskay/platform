{
  "name": "platform",
  "version": "0.2.0",
  "private": true,
  "description": "TypeScript platform template for Next.js apps on AWS serverless.",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "typecheck": "echo 'Platform template has no app source. Apps add their own typecheck script.'",
    "lint": "echo 'Platform template has no app source. Apps add their own lint script.'",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.1",
    "@commitlint/config-conventional": "^21.0.1",
    "@commitlint/types": "^21.0.1",
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.0",
    "prettier": "^3.3.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.60.0"
  }
}
